server:
  port: 9700

mybatis:
  type-aliases-package: com.bailian.service.product.model
  mapper-locations: classpath:mybatis/mapper/*.xml
  config-locations: classpath:mybatis/mybatis-config.xml

mapper:
    mappers:
        - com.bailian.service.product.util.BaseMapper
    not-empty: false
    identity: MYSQL

management:
  security:
    enabled:  false


